machine:
  services:
    - docker

dependencies:
  cache_directories:
    - "~/docker"
  override:
    - mkdir -p ~/docker
    - if [[ -e ~/docker/sentry.tar ]]; then docker load -i ~/docker/sentry.tar; fi
    - docker build -t pocketplaylab/sentry-on-heroku:latest .
    - docker save "pocketplaylab/sentry-on-heroku:latest" > ~/docker/sentry.tar

database:
  override:
    - echo 'No database for testing'

test:
  override:
    - echo 'No test'
deployment:
  production:
    branch: docker
    commands:
      - sed "s/<EMAIL>/$DOCKER_EMAIL/;s/<AUTH>/$DOCKER_AUTH/" dockercfg.template > ~/.dockercfg
      - docker push pocketplaylab/sentry-on-heroku:latest
      - ./deploy.sh :
          timeout: 600
